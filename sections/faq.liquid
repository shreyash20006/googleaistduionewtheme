{{ 'section-faq.css' | asset_url | stylesheet_tag }}

<section class="faq-section">
  <div class="container">
    <div class="section-header">
      <h2 class="section-title">{{ section.settings.heading }}</h2>
      {%- if section.settings.subheading != blank -%}
        <p class="section-subtitle">{{ section.settings.subheading }}</p>
      {%- endif -%}
    </div>

    <div class="faq-grid">
      {%- for block in section.blocks -%}
        <div class="faq-item glass-card">
          <button class="faq-question" aria-expanded="false">
            <span class="faq-question-text">{{ block.settings.question }}</span>
            <span class="faq-icon">
              <svg width="24" height="24" viewBox="0 0 24 24" fill="none">
                <path d="M19 9L12 16L5 9" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
              </svg>
            </span>
          </button>
          <div class="faq-answer">
            <div class="faq-answer-content">
              {{ block.settings.answer }}
            </div>
          </div>
        </div>
      {%- endfor -%}
    </div>

    {%- if section.settings.show_contact_cta -%}
      <div class="faq-cta">
        <p class="faq-cta-text">{{ section.settings.cta_text }}</p>
        <a href="{{ section.settings.cta_link }}" class="btn btn-primary">
          {{ section.settings.cta_button_text }}
        </a>
      </div>
    {%- endif -%}
  </div>
</section>

<style>
  .faq-section {
    padding: 80px 0;
    background: var(--color-white);
  }

  .section-header {
    text-align: center;
    margin-bottom: 60px;
  }

  .section-title {
    font-size: 2.5rem;
    font-weight: 800;
    color: var(--color-navy);
    margin-bottom: 16px;
  }

  .section-subtitle {
    font-size: 1.1rem;
    color: var(--color-dark-text);
    opacity: 0.8;
    max-width: 600px;
    margin: 0 auto;
  }

  .faq-grid {
    max-width: 900px;
    margin: 0 auto;
    display: flex;
    flex-direction: column;
    gap: 20px;
  }

  .faq-item {
    overflow: hidden;
    transition: all 0.3s ease;
  }

  .faq-item:hover {
    box-shadow: 0 12px 32px rgba(0, 0, 0, 0.12);
  }

  .faq-question {
    width: 100%;
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 20px;
    padding: 24px;
    background: none;
    border: none;
    text-align: left;
    cursor: pointer;
    transition: all 0.3s ease;
  }

  .faq-question-text {
    font-size: 1.15rem;
    font-weight: 700;
    color: var(--color-navy);
    flex: 1;
  }

  .faq-icon {
    flex-shrink: 0;
    width: 32px;
    height: 32px;
    border-radius: 50%;
    background: var(--color-light-bg);
    display: flex;
    align-items: center;
    justify-content: center;
    transition: all 0.3s ease;
  }

  .faq-icon svg {
    stroke: var(--color-navy);
    transition: transform 0.3s ease;
  }

  .faq-item.active .faq-icon {
    background: var(--color-green);
  }

  .faq-item.active .faq-icon svg {
    stroke: var(--color-white);
    transform: rotate(180deg);
  }

  .faq-answer {
    max-height: 0;
    overflow: hidden;
    transition: max-height 0.4s ease;
  }

  .faq-item.active .faq-answer {
    max-height: 500px;
  }

  .faq-answer-content {
    padding: 0 24px 24px;
    color: var(--color-dark-text);
    line-height: 1.7;
    font-size: 1rem;
  }

  .faq-cta {
    text-align: center;
    margin-top: 60px;
    padding: 40px;
    background: var(--color-light-bg);
    border-radius: 16px;
  }

  .faq-cta-text {
    font-size: 1.2rem;
    color: var(--color-dark-text);
    margin-bottom: 24px;
    font-weight: 600;
  }

  @media (max-width: 768px) {
    .faq-section {
      padding: 60px 0;
    }

    .section-title {
      font-size: 2rem;
    }

    .faq-question {
      padding: 20px;
    }

    .faq-question-text {
      font-size: 1rem;
    }

    .faq-answer-content {
      padding: 0 20px 20px;
      font-size: 0.95rem;
    }
  }
</style>

<script>
  document.addEventListener('DOMContentLoaded', function() {
    const faqItems = document.querySelectorAll('.faq-item');

    faqItems.forEach(item => {
      const question = item.querySelector('.faq-question');
      
      question.addEventListener('click', () => {
        const isActive = item.classList.contains('active');
        
        // Close all other items
        faqItems.forEach(otherItem => {
          if (otherItem !== item) {
            otherItem.classList.remove('active');
            otherItem.querySelector('.faq-question').setAttribute('aria-expanded', 'false');
          }
        });

        // Toggle current item
        if (isActive) {
          item.classList.remove('active');
          question.setAttribute('aria-expanded', 'false');
        } else {
          item.classList.add('active');
          question.setAttribute('aria-expanded', 'true');
        }
      });
    });
  });
</script>

{% schema %}
{
  "name": "FAQ",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Frequently Asked Questions"
    },
    {
      "type": "textarea",
      "id": "subheading",
      "label": "Subheading",
      "default": "Find answers to common questions about our notes and services"
    },
    {
      "type": "header",
      "content": "Contact CTA"
    },
    {
      "type": "checkbox",
      "id": "show_contact_cta",
      "label": "Show Contact CTA",
      "default": true
    },
    {
      "type": "text",
      "id": "cta_text",
      "label": "CTA Text",
      "default": "Still have questions? We're here to help!"
    },
    {
      "type": "text",
      "id": "cta_button_text",
      "label": "CTA Button Text",
      "default": "Contact Support"
    },
    {
      "type": "url",
      "id": "cta_link",
      "label": "CTA Link"
    }
  ],
  "blocks": [
    {
      "type": "faq_item",
      "name": "FAQ Item",
      "settings": [
        {
          "type": "text",
          "id": "question",
          "label": "Question",
          "default": "How do I download the notes after purchase?"
        },
        {
          "type": "richtext",
          "id": "answer",
          "label": "Answer",
          "default": "<p>After completing your purchase, you'll receive an instant download link via email. You can also access your purchased notes from your account dashboard anytime.</p>"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "FAQ",
      "blocks": [
        {
          "type": "faq_item",
          "settings": {
            "question": "How do I download the notes after purchase?",
            "answer": "<p>After completing your purchase, you'll receive an instant download link via email. You can also access your purchased notes from your account dashboard anytime.</p>"
          }
        },
        {
          "type": "faq_item",
          "settings": {
            "question": "Are the notes updated regularly?",
            "answer": "<p>Yes! We regularly update our notes based on the latest syllabus and exam patterns. All updates are free for existing customers.</p>"
          }
        },
        {
          "type": "faq_item",
          "settings": {
            "question": "Can I get a refund if I'm not satisfied?",
            "answer": "<p>We offer a 7-day money-back guarantee. If you're not satisfied with your purchase, contact our support team for a full refund.</p>"
          }
        },
        {
          "type": "faq_item",
          "settings": {
            "question": "What format are the notes in?",
            "answer": "<p>All notes are provided in high-quality PDF format, optimized for both digital viewing and printing.</p>"
          }
        },
        {
          "type": "faq_item",
          "settings": {
            "question": "Do you offer combo packs or discounts?",
            "answer": "<p>Yes! We offer semester-wise combo packs at discounted prices. Check our Combo Packs section for the best deals.</p>"
          }
        }
      ]
    }
  ]
}
{% endschema %}
